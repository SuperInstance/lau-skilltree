[package]
name = "lau-skilltree"
version = "0.1.0"
edition = "2021"
description = "Skill tree system for LAU — visual progression of abilities"
license = "MIT"

[dependencies]
serde = { version = "1", features = ["derive"] }

[dev-dependencies]
serde_json = "1.0.150"
